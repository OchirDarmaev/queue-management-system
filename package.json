{
  "name": "queue-management-system",
  "version": "1.0.0",
  "description": "The electronic queue system allows you to automate the management of client flows",
  "scripts": {
    "debug": "export IS_LOCAL_DEPLOYMENT=true && export SLS_DEBUG=* && node --inspect node_modules/serverless/bin/serverless offline start --stage dev --verbose --noPrependStageInUrl -t "
  },
  "author": "Ochir Darmaev",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.298.0",
    "@aws-sdk/lib-dynamodb": "^3.298.0",
    "aws-lambda": "^1.0.7",
    "serverless-dynamodb-local": "^0.2.40",
    "ulid": "^2.3.0"
  },
  "devDependencies": {
    "@types/uuid": "^9.0.1",
    "esbuild": "^0.16.17",
    "serverless": "^3.28.1",
    "serverless-auto-swagger": "^2.12.0",
    "serverless-esbuild": "^1.41.0",
    "serverless-offline": "^12.0.4"
  }
}
